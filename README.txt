LOOM v0.1.0 — Systems Engineering Project Control

Serve this directory from a local or hosted web origin. Example:

  python3 -m http.server 4173

Then open http://localhost:4173 in a modern browser.

Project data remains in that browser profile and origin. Use LOOM's full-project JSON export for backups and transfers.
