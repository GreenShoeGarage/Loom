LOOM v0.5.0 — Systems Engineering Project Control

Serve this directory from a local or hosted web origin. Example:

  python3 -m http.server 4173

Then open http://localhost:4173 in a modern browser.

Batch 4 adds method-specific verification activities, reusable setups, parameterized cases, exact as-run revision capture, controlled reruns, structured exceptions, operational validation, and explainable readiness governance across 22 controlled record types.

Project data remains in that browser profile and origin. Use LOOM's full-project JSON export for backups and transfers. Do not rely on file:// for supported IndexedDB, service-worker, offline, or installable-application behavior.
